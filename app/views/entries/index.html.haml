%p#notice= notice

%h1 Entries

%table
  %thead
    %tr
      %th Title
      %th Body
      %th{ colspan: 3 }
  - @entries.each do |entry|
    %tbody
      %tr
        %td= entry.title
        %td= entry.body
        %td= link_to 'Show', entry
        %td= 'Edit'
        %td= 'Destroy'

%br

= link_to 'New Entry', new_entry_path
